import { CoteFormatPipe } from './cote-format.pipe';

describe('CoteFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CoteFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
